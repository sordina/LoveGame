function baddies_random_guy()
	return {
		x       = math.random(),
		y       = math.random(),
		theta   = math.random(),
		display = "!!!"
	}
end

function baddies_update_all(dt)
end

function baddies_update_single(baddie,dt)
end
